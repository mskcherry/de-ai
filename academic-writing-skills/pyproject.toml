[project]
name = "de-ai"
version = "1.0.0"
description = "De-AI: Remove AI fingerprints from academic writing while preserving LaTeX/Typst syntax"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["Pillow>=12.1.0", "PyYAML>=6.0.2", "Jinja2>=3.1.0"]
authors = [{ name = "mskcherry" }]
keywords = [
    "de-ai",
    "ai-detection",
    "academic-writing",
    "latex",
    "paper-writing",
    "grammar-check",
    "format-check",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Education",
    "Topic :: Text Processing :: Markup :: LaTeX",
    "Topic :: Scientific/Engineering",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

[project.optional-dependencies]
dev = ["pytest>=8.4.2", "ruff>=0.15.4", "pyright>=1.1.390"]

[project.urls]
Homepage = "https://github.com/mskcherry/de-ai"
Documentation = "https://github.com/mskcherry/de-ai/tree/main/docs"
Repository = "https://github.com/mskcherry/de-ai"
Issues = "https://github.com/mskcherry/de-ai/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["academic-writing-skills"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]
ignore = [
    "E501", # line too long (handled by formatter)
]

[tool.ruff.lint.isort]
known-first-party = ["academic-writing-skills"]

[tool.pyright]
include = ["academic-writing-skills", "tests"]
exclude = ["**/__pycache__", "**/node_modules", ".venv", "docs"]
pythonVersion = "3.10"
typeCheckingMode = "basic"
reportMissingImports = false
reportMissingModuleSource = false
reportMissingTypeStubs = false
reportAttributeAccessIssue = "warning"
reportArgumentType = "warning"
reportCallIssue = "warning"
reportOperatorIssue = "warning"
reportGeneralTypeIssues = "warning"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
